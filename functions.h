#include <vector>

void displayGridOnly(const std::vector<std::vector<char>> &grid);

void updateAndDisplayGrid(std::vector<std::vector<char>> &grid, char let, std::vector<int> rowCol);

char getLetter();

std::vector<int> getRowColumn();




